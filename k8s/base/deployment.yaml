apiVersion: apps/v1
kind: Deployment
metadata:
  name: stackfood-production
  labels:
    app.kubernetes.io/name: stackfood-production
    app.kubernetes.io/instance: stackfood-production
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: stackfood
    app: stackfood-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: stackfood-production
      app: stackfood-production
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: stackfood-production
        app.kubernetes.io/instance: stackfood-production
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: stackfood
        app: stackfood-production
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8083"
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: production
          image: ghcr.io/stack-food/stackfood-api-production:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
              name: http
              protocol: TCP
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          envFrom:
            - configMapRef:
                name: stackfood-production-config
            - secretRef:
                name: stackfood-production-secrets
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:8083"
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
              scheme: HTTP
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
